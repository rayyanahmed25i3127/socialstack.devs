PORT=5000
# Add your Resend key here — never commit the real .env file
RESEND_API_KEY=
CONTACT_TO_EMAIL=ss.socialstack@gmail.com
# Use onboarding@resend.dev for local testing. In production, replace with a verified Resend domain sender.
CONTACT_FROM_EMAIL=Social Stack <onboarding@resend.dev>
